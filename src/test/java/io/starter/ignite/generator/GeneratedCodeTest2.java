package io.starter.ignite.generator;

import org.junit.Test;

public class GeneratedCodeTest2 {

	@Test
	public void handleRESTAPIREsponse() {
		//
		// if (!(rx instanceof Long)) {
		// List<String> rez = new ArrayList<String>();
		// for (Object o : (List) ret) {
		// Object objectMapper;
		// String z = objectMapper.writeValueAsString(o);
		// rez.add(z);
		// }
		// return new ResponseEntity<List<String>>(rez,
		// HttpStatus.OK);
		// }
	}
}